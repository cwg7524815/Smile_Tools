# Smile Video To Sheet

Pure frontend video grid splitter for frame animation extraction.

## Deploy

Upload these files to GitHub Pages:

- `index.html`
- `style.css`
- `app.js`
- `.nojekyll`

`server.js` is only for local preview:

```bash
node server.js
```
